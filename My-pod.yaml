apiVersion: v1
kind: Pod
metadata:
  name: title
  labels:
    role: title`1
spec:
  containers:
    - name: title
      image: nginx
      imagePullPolicy: IfNotPresent
      ports:
        - name: title
          containerPort: 90
          protocol: TCP
  restartPolicy: Always
  
