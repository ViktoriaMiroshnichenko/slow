apiVersion: v1
kind: Pod
metadata:
  name: title
  labels:
    role: titletitle
spec:
  containers:
    - name: title
      image: nginx
      imagePullPolicy: IfNotPresent
      ports:
        - name: title
          containerPort: 90
          protocol: TCP
  restartPolicy: Always
  
